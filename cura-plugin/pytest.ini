[pytest]
# The plugin package dir (cura-plugin/) has its own __init__.py for Cura's
# loader. Use importlib mode so pytest imports test modules by path instead of
# synthesizing a package from that __init__.py (which does Cura-only imports).
addopts = --import-mode=importlib
testpaths = tests
